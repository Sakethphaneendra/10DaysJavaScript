// Declare a variable called name and assign it the value "John".

var names = "John"
console.log(names)

// Declare three variables: num1, num2, and sum. Assign values of your choice to 
// num1 and num2, then calculate their sum and store it in sum.

var num1 =12
var num2= 15
var sum =num1+num2
console.log(sum)

// Declare a constant variable called PI and assign it the value of π (approximately 3.14).

const PI = 3.14
console.log(PI)

// Declare a variable isRaining and assign it a boolean value indicating whether it's currently raining or not.
var isRaining = false
console.log(isRaining,typeof(isRaining))

// Declare a variable myArray and assign it an empty array.

var myArray = [120,32]
console.log(myArray,typeof(myArray))

// Declare a variable myObject and assign it an empty object.

var myArray2 = {120:32}
console.log(myArray2,typeof(myArray2))

// Declare a variable fullName and assign it your full name as a string.

var fullname = "Kondisetti Phaneendra Saketh"
console.log(fullname)

// Declare two variables width and height, and calculate the area of a rectangle using these variables.

width =12
height = 32
area_rectangle = width*height
console.log(area_rectangle)

// Declare a variable myFunction and assign it a function that takes two parameters and returns their sum.

var myFun = Fun(10,40)
console.log(myFun)
function Fun(a,b){

    return a+b

}